const Meta = (props) => {
    return (
        <head>
            <title>{props.title} - Now</title>
            <meta name="description" content="Now, Home : Chat anywhere, Anytime Seamlessly" />
        </head>
    )
}

export default Meta